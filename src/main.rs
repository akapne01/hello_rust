// Imports file
mod vars;
mod print;
mod primitive_types;
mod strings;
mod greatest_common_divisor;


fn main() {
    primitive_types::run();
    vars::run();
    print::run();
    strings::run();
    greatest_common_divisor::run();
}
